#version 450

layout(binding = 0) uniform UBO
{
    float time;
};

layout(std430, binding = 1) buffer SSBO
{
    vec2 offset;
};

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    offset = vec2(sin(time), cos(time)) * 0.5;
}